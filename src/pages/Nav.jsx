// Already updated for keyboard navigation and ARIA.
// Assuming existing content is fine, we just ensure the links point to correct IDs.
// No change needed from the provided description, so keep as is.